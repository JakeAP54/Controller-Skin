body {
    background-color: #121212;
    color: #ffffff;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
}
.controller {
    position: relative;
    width: 400px;
    height: 250px;
    background: linear-gradient(145deg, #1a1a1a, #2b2b2b);
    border-radius: 20px;
    box-shadow: 5px 5px 15px #0a0a0a, -5px -5px 15px #2a2a2a;
}

/* Left Stick */
.left-stick {
    position: absolute;
    top: 50px;
    left: 50px;
}
.thumb {
    background: #6a0dad;
    border-radius: 50%;
    width: 50px;
    height: 50px;
    box-shadow: 2px 2px 5px #000;
}

/* Right Stick */
.right-stick {
    position: absolute;
    top: 150px;
    left: 250px;
}

/* D-pad */
.dpad {
    position: absolute;
    top: 120px;
    left: 50px;
    display: grid;
    grid-template-rows: 1fr 1fr 1fr;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 5px;
}
.dpad div {
    width: 30px;
    height: 30px;
    background: #3a0d5e;
    box-shadow: inset 2px 2px 5px #1a1a1a, inset -2px -2px 5px #4b0082;
}
.dpad-up { grid-row: 1; grid-column: 2; }
.dpad-left { grid-row: 2; grid-column: 1; }
.dpad-right { grid-row: 2; grid-column: 3; }
.dpad-down { grid-row: 3; grid-column: 2; }

/* Buttons */
.buttons {
    position: absolute;
    top: 70px;
    right: 50px;
}
.button {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    margin: 5px;
    background: #6a0dad;
    box-shadow: 2px 2px 5px #000;
}
.a { background: #0f0; }
.b { background: #f00; }
.x { background: #00f; }
.y { background: #ff0; }

/* Triggers */
.triggers {
    position: absolute;
    top: 10px;
    width: 100%;
    display: flex;
    justify-content: space-between;
    padding: 0 30px;
}
.trigger {
    width: 60px;
    height: 20px;
    background: #3a0d5e;
    border-radius: 10px;
    box-shadow: inset 2px 2px 5px #1a1a1a, inset -2px -2px 5px #4b0082;
}

/* Guide Button */
.guide-button {
    position: absolute;
    top: 90px;
    left: 180px;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: #444;
    box-shadow: inset 2px 2px 5px #1a1a1a, inset -2px -2px 5px #666;
}
